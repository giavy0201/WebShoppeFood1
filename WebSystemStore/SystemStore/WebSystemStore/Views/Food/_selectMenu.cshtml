@foreach (var item in Model)
{
    <div class="form-check my-2">
        <input class="form-check-input" type="checkbox" name="menu" value="@item.Id">
        <label class="form-check-label">@item.Name</label>
    </div>
}